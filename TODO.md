# TradeEye TODO

最后更新：2026-08-24

## 当前状态

`recommend_v2` 确定性荐股、三日虚拟交易、组合净值、双层周报、五维盘后诊断、ETF 可选接口和 GitHub Actions 调度已经实现。当前文件只保留上线观察与后续研究事项；完整决策过程见 `LOG.md`。

## 本轮已完成

- [x] 股票改为统一候选池，使用短线动能 40、收盘质量 35、量能资金 25 的封顶加分；风险门独立于质量分。
- [x] 每日最多发布 5 条但允许少推或不推；20 元及以下仅获得可配置排序偏好，不改变原始质量分；硬价格上下限可关闭。
- [x] 移除 `long_value` 短线正向分和旧价格硬分组；行业只保存并报告，不作为持仓硬限制。
- [x] ETF 默认关闭且关闭时零接口调用；启用后使用白名单、独立 `45/35/20` 评分、版本、报告和信号文件。
- [x] 信号采用稳定 ID、版本化表头、规则指纹、生成时间、首次写入不覆盖和同目录临时文件原子替换。
- [x] 实现 `D close × 98%` 的 D+1 回踩入场、`+4% / -3%` 固定止盈止损、T+1、D+3 超时退出和 0.15 个百分点往返成本。
- [x] 实现 5 个归一化槽位、每日最多 2 笔新仓、同股不重复、盘前容量约束和自然留空；不为满仓递补低质量股票。
- [x] 区分 `entry_unavailable`、未触发、主行情失败、`exit_deferred` 和一字跌停延期；延期持仓保留槽位并用最后有效价做陈旧估值。
- [x] 交易与 NAV 按已有水位增量结算；同一 `as_of` 重跑不请求行情也不改写 CSV，新增交易日只取新增日行情。
- [x] 周报只评价 `recommend`，分别展示信号层和组合层，并按策略版本拆分本周与默认 45 日滚动窗口。
- [x] 跨周信号、触发和实际退出按各自日期归属；开放交易不进入胜率，容量跳过仍保留信号层结果。
- [x] `analysis` 改为趋势 30、价格行为 25、量能 20、资金 15、市场 10；风险矩阵独立，数据不足不评分。
- [x] 全项目移除 LLM 调用、密钥和运行配置；荐股与盘后诊断均使用确定性本地模板。
- [x] 主行情失败时停止推进，辅助行情可显式降级；缓存键、配置校验、退出码、飞书业务响应和 RSS 全源失败路径已经补强。
- [x] Actions 调整为工作日 06:00 荐股、18:00 结算后诊断、周五 19:30 周报，并新增 push/PR CI。
- [x] Actions 持久化 `data/signals/`、`data/trades/` 和 `data/portfolio/`；运行依赖与测试依赖分离。
- [x] 同步 README、`.env.example`、`rules.yaml`、BAT、本地入口和数据文件说明；删除过时的 `file_list.txt`。
- [x] 补齐荐股、ETF、交易状态机、组合、周报、数据降级、通知、配置和工作流测试。

## 验证基线

- [x] `python -m pytest`：148 项通过。
- [x] `python -m compileall -q tradeeye main.py recommend_main.py news_main.py backtest_main.py portfolio_main.py config.py`：通过。
- [x] `git diff --check`：通过。
- [x] 固定假数据覆盖推荐、结算、盘后诊断和周报，不连接真实券商，也不执行真实下单。

## 上线后优先观察

- [ ] 查看首轮 06:00 / 18:00 / 周五 19:30 Actions，确认 Tushare 权限、飞书推送和三类 CSV 提交链路在真实仓库环境正常。
- [ ] 连续观察幂等重跑、GitHub Actions 推送冲突与主/辅行情降级日志，确认失败时没有错误推进账本。
- [ ] 积累足够 `recommend_v2` 样本后，小范围比较最低质量分和已确认风险门；同时报告样本量、触发率和净收益，不做大规模样本内参数搜索。
- [ ] 在 GitHub 仓库设置中手动删除不再使用的 LLM Secrets/Variables；保留 `TUSHARE_TOKEN` 与 `FEISHU_WEBHOOK`。

## 后续研究

- [ ] 样本足够后增加年化收益、波动率、最大回撤和净值曲线；展示时必须同时给出策略版本、实际起止日和样本量。
- [ ] 评估指数基准、最大有利/不利波动（MFE/MAE）和不同回踩参数，确认它们确实改善解释力后再进入主报告。
- [ ] ETF 真正启用并积累样本后，再实现独立 ETF 交易/净值序列；不得与股票胜率或净值混算。
- [ ] 根据组合样本评估是否需要行业槽位上限；当前仅报告行业集中度。

## 低优先级 `news`

- [ ] 持久化最近已发送新闻指纹，完成跨批次去重。
- [ ] 增加各 RSS 来源的成功、失败、抓取数和匹配数详细汇总；全源故障与正常无结果的退出状态已经区分。
- [ ] 使用确定性关键词相关度与发布时间排序，并正确标记未知发布时间。
- [ ] 在出现可交互飞书机器人前，保持 `news` 为独立单向主题简报，不接入评分、交易或周报。
