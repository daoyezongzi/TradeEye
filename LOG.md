# TradeEye LOG

## 2026-08-23｜现状调研与算法改进对接启动

### 本轮范围

- 仅完成仓库调研、现行算法梳理和日志落盘。
- 不修改荐股、尾盘分析、周胜率、配置、工作流或 README 实现。
- 本轮基线为本地 `main` 提交 `3e5472d`；本地 `origin/main` 跟踪引用一致。

### 已验证现状

- 已实现四条工作流：`analysis` 个股复盘、`recommend` 每日荐股、`news` RSS 简报、`backtest` 周胜率回看。
- 使用独立 pytest 临时目录运行测试，结果为 `64 passed`。
- README 随最新本地提交补过回测与规则配置，但仍存在工作流数量、本地回测入口、Actions 变量和 `.env.example` 等遗漏。
- 当前产品不包含券商下单、实时盘口、持仓管理或 UI；它是行情分析、候选推荐、消息推送和轻量回看工具。

### 现行尾盘分析算法基线

尾盘分析从 Tushare 全市场快照和单股近 60 天日线生成特征，再累计本地分数。总分最终限制在 `0-100`。

| 维度 | 当前加分 | 当前减分 |
| --- | --- | --- |
| 市场环境 | 市场分 `>=15`：`+10` | 市场分 `<=-15`：`-15` |
| 均线排列 | 收盘价 `> MA5 > MA10 > MA20`：`+18`；站上 MA5/MA10：`+12`；仅站上 MA5：`+6` | 其余：`-10` |
| MA5 斜率 | `>0.2%`：`+4` | `<-0.2%`：`-4` |
| 收盘位置 | 日内强度 `>=0.8`：`+18`；`>=0.68`：`+10` | `<0.45`：`-15` |
| 当日涨跌幅 | `1.2%-6.5%`：`+12`；`0%-1.2%`：`+5` | `<-1.5%`：`-18`；`>8%`：`-12` |
| K 线实体 | 收阳：`+8` | 未收阳：`-4` |
| 上影线 | `<=1.2%`：`+6` | `>2.5%`：`-10` |
| 换手率 | `2%-12%`：`+10`；`0.8%-2%`：`+4` | `>18%`：`-8`；现行代码的其他区间也进入 `-8` 分支 |
| 成交额/近 5 日 | `1.2-3`：`+10` | `>4`：`-6`；`0-0.8`：`-6` |
| 量比 | `1-2.5`：`+8` | `>4`：`-4`；`0-0.6`：`-4` |
| 资金净流入占比 | `>=3%`：`+14`；`>=1%`：`+8` | `<=-2%`：`-14` |
| 大单净额占比 | `>=2%`：`+12`；`>=0.5%`：`+6` | `<=-1%`：`-12` |
| 近 10 日突破 | 距前高 `-1%` 到 `2.5%`：`+8` | 低于前高超过 `3%`：`-8` |
| 距涨停空间 | 剩余 `2%-7%`：`+6` | 剩余 `0%-1.2%`：`-10` |
| 市场横截面排名 | 换手前 25%：`+4`；资金流前 20%：`+4`；大单前 20%：`+4` | 无对应减分 |
| 风险惩罚 | 无 | ST：`-40`；上市不足 120 天：`-25`；北交所：`-20` |

市场分本身按以下四项累计：上涨家数占比 `>=55%` 加 15、`<=45%` 减 15；涨幅 `>=5%` 的强势股占比 `>=8%` 加 10、`<=3%` 减 5；跌幅 `<=-3%` 的弱势股占比 `>=12%` 减 10、`<=6%` 加 5；资金净流入家数占比 `>=55%` 加 5、`<=45%` 减 5。尾盘个股评分只在该市场分 `>=15` 或 `<=-15` 时再加减个股分。

当前状态分档：

- `>=80`：强候选。
- `65-79`：候选。
- `50-64`：观察。
- `<50`：回避。
- 默认 `>=70` 才调用 LLM；低于 70 使用本地报告。

当前关键时间问题：Actions 在北京时间约 15:30 运行 `analysis`，但数据层在 17:10 前会回退到上一交易日，因此现行“尾盘分析”实际经常分析上一交易日，而不是当天尾盘。

### 现行每日荐股算法基线

基础过滤：仅保留允许交易所、价格大于 0 且不超过 20 元的股票，并排除 ST 和退市标的。任一评分维度命中即可进入候选池，没有额外总分门槛。

1. `short_burst`（昨日短线强势）
   - 命中条件：量比 `>2`、换手率 `5%-15%`、涨幅 `>2%`。
   - 基础分 55；量比最多再加 20；换手率越接近 10% 最多加 15；涨幅最多再加 10。

2. `t_active`（日内活跃）
   - 命中条件：振幅 `>4.5%`、成交额 `>500000`（沿用 Tushare `amount` 单位）。
   - 基础分 50；振幅最多再加 25；成交额最多再加 25。

3. `long_value`（行业内相对价值）
   - 要求 PE、市值和行业信息有效。
   - 行业内低 PE 前 40%，同时行业内高市值前 20% 才命中。
   - 基础分 50；更低 PE 最多加 25；更高市值最多加 25。
   - `RECOMMENDER_INDUSTRIES` 当前只过滤这个维度，不过滤全部荐股候选。

综合分公式：

```text
total_score = short_burst * 0.4
            + t_active * 0.3
            + long_value * 0.3
            + (命中维度数 - 1) * 4
```

最后按 `0-10` 元和 `10-20` 元分组，各取综合分最高的 5 只；同分时成交额优先。随后生成本地简表，并调用 LLM 点评。

### 现行周胜率口径及问题

- 信号只记录行情日期和当日收盘价，没有记录消息生成时间、计划入场时间或规则版本。
- 当前“隔夜收益”定义为：`下一交易日开盘 / 信号日收盘 - 1`。
- 当前“T+1 全天收益”定义为：`下一交易日收盘 / 信号日收盘 - 1`。
- 正收益即记为胜，按复盘分档或荐股价格组统计胜率和平均收益。

该口径不能直接回答“昨日强势股在次日正常回落后，是否仍然值得关注”：

- 每日好股在次日 06:00 左右才发出，却使用昨日收盘作为回测入场价，存在不可成交区间。
- 昨日强势股次日小幅回落会被直接计为失败，未区分健康回踩、冲高机会、相对指数强弱和后续 3-5 日表现。
- 价格组胜率不能说明 `short_burst`、`t_active`、`long_value` 各自是否有效。

### 用户已确认的方向

- 认可上一轮调研中的工程优化建议。
- 荐股算法和尾盘分析算法需要先解释、对接，再决定如何调整加减分。
- 周胜率需要改进，不能机械地把昨日强势股次日回落视为策略失败。
- 当前阶段只对接和落盘，不实施业务代码改动。

### 待共同决定

- “每日好股”的实际使用方式、预期入场时间和持有周期。
- 荐股是寻找次日延续、次日回踩后的低吸机会，还是 3-5 日强势观察池。
- 尾盘分析究竟要成为收盘前可执行的隔夜信号，还是盘后生成的次日计划。
- 周报的基准、入场价、观察周期、成功条件、最大容忍回撤和分组维度。
- 完成上述定义后，再形成实现计划、测试标准和迁移方案。

## 2026-08-23｜日志命名纠正与每日好股目标初步确认

### 日志约定纠正

- 项目此前没有既有日志，按 `developing` 技能约定使用 `LOG.md` 和 `TODO.md`。
- 已放弃误用的 `DEVELOPMENT_HISTORY.md` 和 `TASK_STATUS.md` 命名；内容迁移到标准文件，不保留重复日志。

### 用户新确认的信息

- 每日好股用于短线进出。
- 初步使用方式是等待次日回踩后进入，逢高卖出，而不是机械追涨或长期持有。
- 需要重点防范“只强一天、随后持续转弱”的一日游或疑似诱多标的。
- 上述内容是策略目标，不代表任何过滤条件、阈值或收益假设已经成立；必须通过历史样本验证。

### 当前研究假设（未定案）

- 每日好股的评价不能只看“次日收盘是否高于昨日收盘”，而应围绕可执行的次日入场和短线退出机会。
- 正常回踩与结构性转弱需要分开：回踩后仍有承接、相对强度或后续冲高机会，不应直接判败；放量冲高回落、资金背离、收盘失去强势结构等现象可能成为风险信号。
- 对疑似一日游标的可以设计“硬性否决”和“风险降级”两层，但采用哪一层、允许多少漏选，尚未决定。
- 荐股指标将来至少应分开统计次日可执行入场后的盘中机会、最大不利波动和 1-3 日结果；具体观察窗口仍待确认。

### 本轮约束

- 只纠正日志和记录对接信息。
- 不修改荐股、尾盘分析、周胜率或任何业务代码。

## 2026-08-23｜接受稀疏推荐，不强制凑满 Top5

### 已确认决策

- 用户接受为了降低一日游误推而明显减少每日推荐数量。
- `0-10` 元组、`10-20` 元组都允许少于 5 只，极端情况下允许为空。
- Top5 只表示每组数量上限，不再表示必须输出 5 只。
- 策略取向优先提升候选质量，不以推荐数量稳定为目标。

### 对未来算法的直接约束

- 排名之前必须先经过最低质量门槛；未过门槛的股票不能因为组内候选不足而递补。
- 一日游风险不能仅靠总分排序稀释，需要有独立的风险识别层。
- 明确风险可作为硬性否决；不确定风险可以降低等级并要求次日确认。具体特征和阈值仍需回测后决定。
- 周报必须同时展示“产生了多少候选”和“入选候选表现”，避免通过极端缩小样本制造虚高胜率。

### 下一项设计分支

需要明确 TradeEye 对“次日回踩确认”承担到什么程度。当前系统只有收盘后/早晨批处理和日线数据，无法直接确认盘中回踩的先后顺序与承接情况；若要求自动确认，将引入盘中数据、第二次运行和新的推送时点。

### 本轮约束

- 只记录决策，不修改任何业务代码或规则文件。

## 2026-08-23｜每日荐股改为全自动策略闭环

### 范围澄清

- 当前对接聚焦于 `recommend` 每日荐股的重新定义。
- 仓库原有 `analysis` 个股复盘、`news` 简报和 `backtest` 周回看仍然存在，但本轮尚未重新设计它们；尾盘分析的新算法也尚未开始定案。
- 此处所说的“周胜率”只先确定要与每日荐股共用同一套交易规则，具体指标和实现仍待后续对接。

### 已确认的策略方向

- 不把人工盘中确认作为每日荐股的主流程；目标改为一套可自动执行、可自动回放、结果可复现的确定性策略。
- 每日荐股、条件入场、条件退出和周度统计必须使用同一策略版本，避免推荐逻辑与胜率口径彼此脱节。
- 推荐仍允许少于每组 5 只或为空；没有通过质量与风险门槛的股票不得为凑数而递补。

### 自动策略骨架（阈值未定）

1. 在信号日 `D` 生成候选：先做基础质量过滤和一日游风险识别，再评分排序。
2. 在后续约定的入场窗口内，用客观价格与成交条件判断是否触发；未触发的候选记为“未入场”，不能按假定买入价计算输赢。
3. 入场后使用同一套确定性止盈、止损和超时退出规则；每笔模拟交易保留信号时间、触发时间、成交假设、退出原因和策略版本。
4. 周报同时展示信号数、入场触发率、实际入场交易的胜率与平均收益，并保留未触发数，防止通过缩小分母制造虚高胜率。
5. 后续应补充盈亏比、期望收益、最大有利波动和最大不利波动；单独一个“胜率”不足以评价策略质量。

### 数据与回测约束

- 仅用日线 OHLC 无法判断同一交易日内是先回踩触发买入，还是先冲高后下跌，也无法可靠复现“回踩买入后逢高卖出”的先后顺序。
- 后续需要在“引入分钟级行情”与“先采用明确且保守的日线成交假设”之间选择；在选择前，不把任何模拟胜率描述为已验证收益。
- 当前只记录策略契约，不修改 Python、规则配置、工作流或 README。

### 下一项设计分支

先确定触发入场后的最大持有期。这个参数会决定退出规则、所需行情粒度、样本归属和周报统计窗口，然后再继续讨论具体回踩条件和一日游否决特征。

## 2026-08-23｜每日荐股最大持有期确定为 3 个交易日

### 已确认决策

- 每日荐股策略在触发买入后最多持有 3 个交易日。
- 按当前讨论口径，若 `D+1` 触发买入，则 `D+1` 计为持有第 1 日，最晚在 `D+3` 退出；止盈或止损可以使交易提前结束。
- 该参数是待历史回测的初始策略设定，不代表 3 日持有已经被数据证明优于 1 日或 5 日。

### A 股交易约束

- 上交所现行有效的《上海证券交易所交易规则（2026 年修订）》第 3.1.4 条规定，投资者买入的证券在交收前不得卖出，实行回转交易的品种除外；第 3.1.5 条列出的当日回转交易品种不包括 A 股：https://www.sse.com.cn/lawandrules/sselawsrules2025/stocks/exchange/c/c_20260424_10816482.shtml
- 因此，`D+1` 新买入的股票不能在 `D+1` 盘中“逢高卖出”，最早可执行退出日是 `D+2`。
- 若买入当日已经跌破理论止损线，策略也不能假设按该止损价当日成交；回测必须保留隔夜跳空和次日实际可成交价风险。

### 尚未确定

- 是否引入分钟级行情来识别“先回踩、后企稳/反弹”的顺序。
- 入场触发条件、止盈条件、止损条件，以及 `D+2`、`D+3` 内多个条件同时出现时的成交优先级。
- `D+1` 没有触发入场时是立即让信号过期，还是允许延后触发；当前目标仍偏向次日回踩，暂不扩大入场窗口。

### 本轮约束

- 只记录用户决策和交易制度对策略的约束，不修改业务代码。

## 2026-08-23｜按每日 1-2 次 GitHub Actions 收敛为批处理策略

### 运行约束与现状证据

- 用户明确要求策略考虑项目每天只运行 1 次或 2 次 GitHub Actions，不按持续运行的盘中服务设计。
- 当前主工作流已有两个时点：北京时间约 `06:00` 执行 `recommend`，约 `15:30` 执行 `analysis`；计划任务还会随机延迟 `0-29` 分钟。
- 现有数据层在北京时间 `17:10` 前可能仍回退到上一交易日，因此当前 `15:30` 批次不能可靠承担“用当天完整日线结算策略状态”的职责。未来若采用第二个结算批次，应安排在当日数据稳定可用之后；具体时间尚未实施。

### 设计收敛

- 不采用 5 分钟行情持续监听，也不把“盘中回踩后再确认反弹”设为必要条件。
- 每日荐股改为批处理兼容的条件策略：盘前根据已完成的 `D` 日行情，一次性给出 `D+1` 的入场价、取消条件、止盈参考、止损参考和有效期。
- 第二个批次如保留，用于在收盘数据就绪后更新“未触发、已入场、持有中、止盈、止损、超时退出”等状态，而不是盘中追踪。
- TradeEye 在当前产品边界内自动生成和回放策略，不连接券商、不自动下单；自动化指规则判定与统计可复现。

### 推荐的日线回放约定（待阈值确认）

1. `D+1` 开盘先检查取消条件；出现明显不利跳空时直接取消，不假设接飞刀成交。
2. 若开盘价已经低于或等于预设入场价且未命中取消条件，按开盘价成交；否则只有当日最低价触及入场价时，才按预设入场价成交。
3. `D+1` 未触及入场条件则信号过期，不延长到 `D+2` 追入。
4. 因 A 股 `T+1`，入场当日只记录风险，不模拟卖出；从 `D+2` 开始判断退出。
5. `D+2` 或 `D+3` 同时触及止盈与止损但日线无法确定先后时，按止损先发生处理；跳空越过阈值时按开盘价处理。
6. 到 `D+3` 仍未触发其他退出条件，按 `D+3` 收盘价强制退出。

这些成交约定优先避免回测乐观偏差，但目前只是设计规则，尚未通过历史数据验证。

### 下一项设计分支

确定盘前给出的入场价模型。最小方案是按 `D` 日收盘价固定回撤一定比例；另一方案是根据个股近期波动率动态调整。前者更透明，后者更适应不同股票，但参数更多、解释和防止过拟合更困难。

## 2026-08-23｜固定回撤入场与持仓、费用问题进入设计

### 已确认决策

- `D+1` 入场价采用相对 `D` 日收盘价的固定回撤模型，不采用波动率自适应模型。
- 固定回撤的具体比例和不利跳空取消阈值尚未确定，必须作为可配置参数并通过历史样本比较。

### 现有脚本能力核查

- 当前 `recommend` 最多可产生两个价格组、每组 5 条信号，但信号文件只记录日期、代码、分组、分数、维度和信号日收盘价。
- 当前 `backtest` 把每条信号互相独立地计算“信号日收盘至下一日开盘/收盘”的收益，没有现金余额、持仓数量、同时持仓上限、买卖先后、费用或滑点。
- 因此现有脚本现在不能回答“实际买了哪些”和“账户扣费后赚了多少”，但 Python 和当前日线数据足以实现一个轻量虚拟账户；主要工作是补齐确定性规则和交易账本，不需要券商接口或常驻服务。

### 推荐的两层评估模型（待确认）

1. **信号层**：所有通过固定回撤并触发入场的候选都独立回放，用来评价荐股算法本身；未触发者单列，不因虚拟账户资金不足而从算法样本中消失。
2. **组合层**：不默认买入全部推荐。设置固定的同时持仓上限与等额仓位槽；当多个候选同日触发时，按荐股总分从高到低占用空槽，其余记为“因仓位上限跳过”。
3. 同一股票已有持仓时不重复加仓；空槽保留现金，不为保持满仓而买入低质量候选。
4. 信号层报告触发率、胜率和单笔期望；组合层报告净值、净收益、最大回撤、持仓利用率与扣费总额。两层不得混成一个“周胜率”。

### 最小费用模型

- 短线只持有 3 个交易日，交易成本可能足以改变小幅盈利交易的输赢，因此组合净收益应扣费。
- 券商佣金率及每笔最低佣金因账户而异，不能在策略中写成唯一“官方默认值”；应作为配置输入，并明确是否已包含经手费等项目，防止重复计算。
- 中国结算 2025 年沪、深、北市场收费表均列明，A 股交易过户费按成交金额的 `0.01‰` 向买卖双方收取；实现时应按交易所配置，而不是只覆盖沪市：[上海市场](https://www.chinaclear.cn/zdjs/fbzyls/202506/9d22b74d9f2e40edb67b44d1f6596f18/files/%E4%B8%8A%E6%B5%B7%E5%B8%82%E5%9C%BA%E8%AF%81%E5%88%B8%E7%99%BB%E8%AE%B0%E7%BB%93%E7%AE%97%E4%B8%9A%E5%8A%A1%E6%94%B6%E8%B4%B9%E5%8F%8A%E4%BB%A3%E6%94%B6%E7%A8%8E%E8%B4%B9%E4%B8%80%E8%A7%88%E8%A1%A8.pdf)、[深圳市场](https://www.chinaclear.cn/zdjs/fbzyls/202506/ab6384ba25514554a7eceaee3e521032/files/%E6%B7%B1%E5%9C%B3%E5%B8%82%E5%9C%BA%E8%AF%81%E5%88%B8%E7%99%BB%E8%AE%B0%E7%BB%93%E7%AE%97%E4%B8%9A%E5%8A%A1%E6%94%B6%E8%B4%B9%E5%8F%8A%E4%BB%A3%E6%94%B6%E7%A8%8E%E8%B4%B9%E4%B8%80%E8%A7%88%E8%A1%A8.pdf)、[北京市场](https://www.chinaclear.cn/zdjs/fbzyls/202506/69144099e23c436ab678b9d778260402/files/%E5%8C%97%E4%BA%AC%E5%B8%82%E5%9C%BA%E8%AF%81%E5%88%B8%E7%99%BB%E8%AE%B0%E7%BB%93%E7%AE%97%E4%B8%9A%E5%8A%A1%E6%94%B6%E8%B4%B9%E5%8F%8A%E4%BB%A3%E6%94%B6%E7%A8%8E%E8%B4%B9%E4%B8%80%E8%A7%88%E8%A1%A8.pdf)。
- 同一收费表列明证券交易印花税按成交金额的 `1‰` 向出让方收取并减半征收，即现行计算为卖出金额的 `0.5‰`；财政部、税务总局的减半公告自 2023-08-28 起实施：https://www.mof.gov.cn/jrttts/202308/t20230828_3904235.htm
- 第一版可以先计算佣金、最低佣金、卖出印花税和双边过户费；滑点单独作为可选敏感性参数，默认是否启用尚未决定。

### 实现复杂度判断

- 费用公式本身简单；真正新增的复杂度来自“有多少钱、每个仓位多少钱、最多持有几只、同日多个触发买谁”。
- 一旦给定初始虚拟资金、同时持仓上限和券商佣金参数，脚本即可按交易日顺序确定性重放，GitHub Actions 每日运行 1-2 次足够。
- 当前仍处于调研和规则对接阶段，未修改任何业务代码、配置或数据结构。

### 下一项设计分支

先确定虚拟账户最多同时持有几只股票。建议第一版使用 3 个等额仓位槽：只买已经触发固定回撤条件的候选，按总分排序填充空槽，而不是把每日推荐全部买入。

## 2026-08-23｜同时持仓上限调整为 5 只并保留现金

### 已确认决策

- 虚拟组合最多同时持有 5 只股票，不采用此前建议的 3 只上限。
- 5 只是容量上限，不是持仓目标；候选未通过质量门槛、未触发固定回撤入场或没有盘前空槽时，都不为了满仓而买入。
- 已有持仓继续按原定 3 日退出规则占用槽位，不因出现新的高分推荐而被强制换出。
- 同日可入场候选多于空槽时，按荐股总分从高到低选择；其余候选仍进入信号层评价，但组合层记为“仓位上限跳过”。

### 建议的第一版资金结构（待确认）

- 每只股票使用账户权益的 `15%` 作为目标仓位，最多 5 只，因此即使 5 个槽位全部占用，目标总仓位也不超过 `75%`。
- 至少保留约 `25%` 现金，用于手续费、跳空误差和后续机会，不借钱、不加杠杆。
- 若只有 1-4 只合格并触发的股票，剩余资金继续为现金，不把单只仓位自动放大，也不把空槽分摊给已有持仓。
- 仓位比例以后可以作为回测参数比较，但第一版不按分数动态加减仓，避免同时校准选股分数和资金权重导致解释困难。

### 批处理下的保守槽位规则

- 盘前以当日开始时的持仓确定可用槽位。
- 不假设当天卖出后还能在同一天把腾出的槽位交给另一条新信号；这避免日线数据无法判定买卖先后顺序造成的乐观回测。
- 仓位退出后，从下一交易日开始恢复为空槽。

### 当前约束

- 以上只完成组合规则对接；`15% × 5 + 25%` 仍是待用户确认的推荐方案。
- 未修改业务代码、配置、工作流或信号数据结构。

### 下一项设计分支

确认每只目标仓位 `15%`、组合最高 `75%`、至少保留 `25%` 现金的资金结构。确认后再确定虚拟账户初始资金，以便计算整手数量和最低佣金的真实影响。

## 2026-08-23｜纠正“留子弹”的含义：质量留仓而非固定现金比例

### 用户澄清

- 用户所说的“不总是满仓”不是要求组合永久保留 `25%` 现金。
- 真正要求是：最多允许持有 5 只，但相对较差的候选即使有空槽也可以不买，把未使用的仓位留给后续交易日可能出现的更好标的。
- 因此上一节提出的“每只固定 `15%`、组合最高 `75%`、永久保留 `25%`”方案失效，不再作为当前设计方向。

### 修正后的持仓逻辑

1. 5 只只是同时持仓硬上限，不是每日或组合的配额。
2. 候选必须先通过独立的绝对质量门槛，再通过 `D+1` 固定回撤触发条件，才获得组合入场资格。
3. 总分只负责在多个合格候选之间排序，不能让未过质量门槛的股票因为存在空槽而递补。
4. 没有足够好且触发的股票时，空槽自然保持为现金；如果确有 5 只都达到标准，策略原则上允许使用全部 5 个槽位，并不强制永久留出固定现金比例。
5. 已有持仓仍按 3 日退出规则占用槽位，不因第二天出现新股而提前换仓。

### 可选的跨日预留机制（待确认）

- 若只依靠质量门槛，当天出现 5 只合格股票时仍可能一次占满全部槽位。
- 若希望明确避免单日信号占满组合，可以增加“每天最多新开 2 只”的限制：先过绝对门槛，再从当日已触发者中选分数最高的最多 2 只。
- 该限制能分散入场日期、为后续交易日保留槽位，但也可能跳过同一天出现的第 3 只优质股票；它属于资金节奏规则，不代表选股算法判定其质量较差。

### 当前约束

- 只纠正组合设计与日志状态；尚未确定每日新开仓上限、单槽资金比例或虚拟初始资金。
- 未修改业务代码、配置、工作流或信号数据结构。

### 下一项设计分支

决定是否增加“每天最多新开 2 只”的跨日预留规则。建议采用这一上限，让最多 5 只的三日持仓组合不会被单日行情一次占满。

## 2026-08-23｜确认每天最多新开 2 只

### 已确认决策

- 虚拟组合继续维持最多同时持有 5 只，并增加每天最多新开 2 只的限制。
- “最多 2 只”同样不是配额：当日只有 1 只通过绝对质量门槛并触发固定回撤，就只新开 1 只；没有合格触发则不开仓。
- 当日合格触发超过 2 只且空槽充足时，组合层按荐股总分选择最高的 2 只；其余仍进入信号层评价，并记录为“当日新开仓上限跳过”。
- 实际可新开数量取以下三者最小值：当日合格触发数、每日上限 2、盘前空槽数。例如已有 4 只时最多再开 1 只，已有 5 只时不开新仓。
- 未使用的当日名额不累计到下一交易日；已有持仓也不因新信号提前换出。

### 设计作用

- 该规则把最多 5 只的组合分散到不同信号日，降低单日市场风格或数据异常一次占满全部持仓的风险。
- 它是组合资金节奏约束，不改变荐股算法对被跳过候选的质量评价，因此周报仍需分别展示信号层与组合层结果。
- 当前只记录决策，未修改业务代码、配置、工作流或信号数据结构。

### 下一项设计分支

确定固定回撤比例。建议先以相对 `D` 日收盘价回撤 `2%` 作为可解释的第一版基线，并在历史回测中同时展示 `1%`、`2%`、`3%` 的敏感性结果；在实验前不声称 `2%` 最优。

## 2026-08-23｜固定回撤基线确定为 2%

### 已确认决策

- `D+1` 计划入场价确定为 `D` 日收盘价的 `98%`，即固定回撤 `2%`。
- `2%` 是第一版策略基线，不预设为最优参数；未来回测应在同一信号样本上同时展示 `1%`、`2%`、`3%` 的触发率、净收益、回撤和样本量。
- 价格实现时需要按对应市场的最小报价单位取整，取整方向与成交假设需在实施计划中明确。

### 日线触发含义

- 若 `D+1` 开盘价高于计划入场价，但当日最低价触及该价，则日线模型按计划入场价成交。
- 若 `D+1` 开盘价已经低于或等于计划入场价，则是否按开盘价成交，取决于下一步要确定的不利跳空取消阈值。
- 若全天未触及计划入场价，则记为未触发；信号是否当日直接过期仍需最终确认。

### 当前约束

- 只记录参数决策，不修改业务代码、配置、工作流或数据结构。

### 下一项设计分支

确定不利跳空取消阈值。建议第一版规定：若 `D+1` 开盘价低于或等于 `D` 日收盘价的 `96%`（低开至少 `4%`），取消入场；对 `3%`、`4%`、`5%` 阈值做并列回测，不预设 `4%` 已经最优。

## 2026-08-23｜按每日 2-3 次 Actions 再次简化第一版策略

### 用户约束

- 项目每天只运行 2-3 次 GitHub Actions，第一版不应设计成盘中交易系统，也不应为了回测引入过多撮合与账户参数。
- 因此上一节提出的“低开 `4%` 取消并对多个阈值做敏感性比较”暂不进入第一版；不再继续增加盘中判断分支。

### 第一版最小运行模型

1. 早晨批次根据已完成的 `D` 日数据生成候选和固定入场价 `D 收盘价 × 98%`。
2. 收盘后数据就绪的批次只读取 `D+1` 日线：若最低价触及计划入场价，则记为触发；成交价按 `min(D+1 开盘价, 计划入场价)` 计算。
3. 不设置额外的低开取消阈值，不要求分钟级“回踩后企稳”确认，也不在盘中再次运行策略。
4. `D+1` 全天未触及计划入场价则信号直接过期，不延长到后续交易日追入。
5. 组合仍执行最多同时持有 5 只、每天最多新开 2 只、绝对质量门槛优先且不强制填满。
6. 持仓与退出状态由后续早晨或收盘后批次根据日线推进；每周报告只汇总已经记录的确定性结果。

### 简化后的组合与费用口径

- 第一版使用 5 个归一化仓位槽，不模拟具体初始资金、整手数量或最低佣金；空槽等同现金，单槽收益按组合的 `1/5` 权重计入。
- 交易成本第一版只保留一个可配置的往返成本比例，从每笔已退出交易收益中统一扣除；不拆解成逐项账户账单。该比例具体取值仍待确定。
- 这种模型能回答策略信号和受限组合的相对表现，但不能冒充某个真实券商账户的精确收益。

### Actions 适配

- 两次运行即可覆盖“早晨生成 + 晚间更新”；若保留第三次运行，可承载现有复盘或其他批处理，不用于盘中监听。
- 现有 `15:30` 批次早于完整日线稳定时点，实际实现时需要将状态更新放到数据可用后的批次，或者留到下一日早晨处理。

### 当前约束

- 只收敛设计与日志，不修改业务代码、配置、工作流或数据结构。

### 下一项设计分支

后续只需确定一组简单的固定止盈、固定止损和 `D+3` 超时退出规则，不再增加盘中指标组合。

## 2026-08-23｜第一版固定退出规则提案

### 待确认基线

- 止盈价建议设为入场价的 `104%`，即盈利 `4%`。
- 止损价建议设为入场价的 `97%`，即亏损 `3%`。
- `D+1` 为买入日，受 A 股 `T+1` 约束不执行卖出；从 `D+2` 开始判断止盈或止损。
- 到 `D+3` 仍未触发止盈或止损，则按 `D+3` 收盘价退出。
- `+4% / -3%` 只是便于解释和实现的第一版研究基线，尚无回测证据证明其有效或最优。

### 最小日线判断顺序

1. 若可卖出日开盘价低于或等于止损价，按开盘价退出。
2. 否则若开盘价高于或等于止盈价，按开盘价退出。
3. 若当日日线同时触及止损价和止盈价、无法判断盘中先后，则保守地按止损先发生。
4. 仅触及一个阈值时，按对应阈值退出。
5. 未触及任何阈值且到达 `D+3`，按收盘价退出。

### 明确不进入第一版

- 不做移动止盈、分批卖出、补仓、均线退出、盘中反转确认或动态止损。
- 不为这组参数做大规模网格优化；先用一个基线检查策略是否具备继续研究的价值。
- 当前只记录提案，不修改业务代码、配置、工作流或数据结构。

### 下一项设计分支

确认是否采用固定止盈 `+4%`、固定止损 `-3%`、`D+3` 收盘超时退出。

## 2026-08-23｜固定退出基线已确认

### 已确认决策

- 第一版采用固定止盈 `+4%`、固定止损 `-3%`；`D+3` 仍未触发阈值时按当日收盘价退出。
- `D+1` 为买入日，遵守 A 股 `T+1`，当日即使触及止盈或止损也不卖出；从 `D+2` 开始判断退出。
- 可卖出日若开盘直接越过阈值，按开盘价退出；因此隔夜跳空时，实际亏损可能超过名义 `-3%` 止损。
- 同一根日线同时触及止盈和止损且无法确定先后时，按止损先发生处理；只触及一个阈值时按对应阈值退出。
- 该参数组只是简单、可复现的第一版研究基线，尚未经过历史回测验证，不宣称有效或最优。

### 当前边界

- 不加入移动止盈、分批卖出、补仓、均线退出或分钟级判断。
- 当前只确认策略口径并落盘，未修改业务代码、配置、工作流或数据结构。

### 下一项设计分支

确定统一往返交易成本比例。为适配 5 个归一化仓位槽和每天 2-3 次批处理，建议第一版按每笔已退出交易一次性扣除 `0.15%`，同时保留毛收益与扣费后净收益；该数值只是统一研究假设，不代表任何具体券商账户的精确费率。

## 2026-08-23｜统一往返交易成本已确认

### 已确认决策

- 第一版统一往返交易成本确定为 `0.15%`，不拆分佣金、最低佣金、印花税、过户费和滑点。
- 每笔交易只在退出并结算时扣除一次，净收益率计算为 `退出价 / 入场价 - 1 - 0.0015`。
- 止盈、止损仍按未扣费的价格变化触发；因此恰好按 `+4%` 止盈时，简化后的净收益约为 `+3.85%`，恰好按 `-3%` 止损时约为 `-3.15%`。
- 未触发入场的推荐不产生交易成本；尚未退出的持仓不计入已实现净收益。
- 报告同时保留毛收益和净收益，策略比较默认使用净收益。
- `0.15%` 是统一研究假设，不代表任何具体券商账户或实际成交的精确费用。

### 当前边界

- 不模拟具体初始资金、整手数量、最低佣金或不同券商费率。
- 当前只确认统计口径并落盘，未修改业务代码、配置、工作流或数据结构。

## 2026-08-23｜信号层与组合层双层统计提案

### 待确认设计

为避免“最多持有 5 只、每日最多新开 2 只”反过来扭曲对荐股算法的评价，周报建议并列保留两层结果：

1. **信号层**：评价当日发布且通过绝对质量门槛的全部推荐。凡 `D+1` 触发 `2%` 回撤入场的推荐，都独立套用相同的 `+4% / -3% / D+3` 退出及 `0.15%` 成本规则；即使它因组合满仓或每日开仓上限而未被组合买入，也保留为一笔信号层模拟交易。
2. **组合层**：只评价实际通过“最多 5 个槽位、每日最多新开 2 只、按分数竞争空槽”约束后纳入虚拟组合的交易，用于衡量这套简化资金管理规则的可执行表现。

### 周报最小口径

- 推荐总数、`D+1` 入场触发数和入场触发率单独展示；未触发入场的推荐不算亏损交易。
- 信号层胜率分母只包含本周已经退出的信号层交易；组合层胜率分母只包含本周已经退出的组合层交易。
- 本周末仍持有的交易列为“未结算”，顺延到实际退出所在周，不提前判输赢。
- 两层均展示已结算笔数、盈利笔数、胜率、平均净收益；组合层另展示空槽率及因满仓或每日上限跳过的数量。
- 胜负以扣除 `0.15%` 成本后的单笔净收益是否大于 `0` 判断，而不是用荐股次日相对推荐日收盘的涨跌判断。

### 设计作用与限制

- 信号层回答“荐股和交易规则本身是否有效”，组合层回答“受简单仓位约束后能否形成可执行组合”，两者不再混成一个周胜率。
- 该设计仍可由每天 2-3 次 Actions 基于日线状态推进，不需要盘中监听或真实账户撮合。
- 当前只是待确认提案，未修改业务代码、配置、工作流或数据结构。

### 下一项设计分支

确认是否采用上述双层统计；确认后再讨论疑似“一日游”强势股的简单风险过滤，避免同时改变统计口径和选股规则。

## 2026-08-23｜README 增加功能全景与策略设计边界

### 用户要求

- README 需要罗列 TradeEye 的主要功能，让首次阅读者能直接知道项目现有能力。
- 本轮已经对接的荐股策略与周报口径也需要进入 README，作为后续实现依据。

### 文档调整

- 新增四条现有工作流及规则配置、自动化能力的功能总表。
- 明确当前不包含券商下单、实时盘口、真实账户、持仓账本或 Web UI。
- 新增“荐股策略改版（设计中，尚未实现）”，记录已确认的回撤入场、仓位槽、持有期、退出和统一成本规则。
- 双层统计仍标为待确认设计；当前回测仍明确描述为信号日收盘至次日开盘/收盘，未将设计方案冒充为现成功能。
- 补充本地 `backtest_main.py` 运行入口，并将 Actions 工作流文件数量由两条纠正为三条。

### 当前边界

- 本轮只修改 README 与调研日志，没有修改 Python 业务代码、规则配置、Actions、BAT 或环境变量样例。

## 2026-08-23｜信号层与组合层双层统计已确认

### 已确认决策

- 周报并列保留信号层和组合层，不再把荐股算法质量与仓位约束后的组合表现混成一个胜率。
- 信号层覆盖所有发布、通过绝对质量门槛且在 `D+1` 实际触发回撤入场的推荐；即使因组合满仓或每日最多新开 2 只而未纳入组合，也继续按统一退出规则模拟。
- 组合层只覆盖实际通过 5 个仓位槽、每日最多新开 2 只和分数排序后纳入虚拟组合的交易。
- 未触发入场的推荐只计入推荐数、触发数和触发率，不算输赢；未退出交易保持未结算，等实际退出所在周再进入胜率分母。
- 两层胜负都以扣除 `0.15%` 统一成本后的单笔净收益是否大于 0 判断，并保留已结算笔数、盈利笔数、胜率和平均净收益。
- 组合层另展示空槽率，以及因满仓或每日开仓上限被跳过的数量与原因。
- README 中的双层统计状态已经从“待确认”改为“已确认”，但仍明确标注整套荐股改版尚未实现。

### 当前边界

- 当前只确认统计契约，没有修改信号 CSV、交易状态、回测代码或 Actions。

## 2026-08-23｜疑似一日游风险门提案

### 可直接使用的现有数据

- 当前全市场快照已经计算 `close_strength`（收盘在日内区间的位置）和 `upper_shadow_pct`（上影线占昨收比例），并包含涨跌幅、换手率和量比。
- 第一版风险门可以只使用这些 `D` 日日线字段，不增加 Tushare 查询、不引入分钟线，也不增加 GitHub Actions 运行次数。

### 建议的最小两级规则

1. **冲高回落硬性剔除**：若 `close_strength < 0.45` 且 `upper_shadow_pct > 2.5%`，不进入荐股排名。
2. **过热标记**：分别检查 `pct_chg > 8%`、`turnover_rate > 18%`、`volume_ratio > 4` 三项。
3. **两项及以上过热**：硬性剔除，不因候选不足递补。
4. **恰好一项过热**：降为风险候选；仍需通过绝对质量门槛，但在排序时永远排在普通候选之后，并在推荐和周报中显示风险标签。
5. **没有命中**：按普通候选继续参与质量门槛与总分排序。

### 选择理由与限制

- 阈值复用现行 `analysis` 规则中的弱收盘、长上影和过热边界，避免第一版再引入一套更复杂的指标。
- “先分风险层、层内再按分数排序”不依赖尚待重做的总分尺度，也比临时扣一个任意分数更容易解释。
- 该风险门只能识别日线上的冲高回落或过热形态，不能证明某只股票存在操纵，也不能保证识别所有次日持续下跌标的；报告统一使用“一日游/过热风险”，不作“杀猪盘”定性。
- 所有阈值均为待历史回测的第一版假设，需要在信号层比较剔除前后的样本数、触发率、平均净收益和最大不利波动，不预设过滤一定有效。
- 当前只是待确认提案，未修改业务代码、规则配置、README 策略表或 Actions。

### 下一项设计分支

确认是否采用上述“明显冲高回落或两项过热硬剔除、单项过热降级”的最小风险门。

## 2026-08-23｜一日游风险门已确认，并恢复全局优化视角

### 已确认决策

- 第一版接受“明显冲高回落或两项过热硬剔除、单项过热降级”的风险门。
- `close_strength < 0.45` 且 `upper_shadow_pct > 2.5%` 时直接剔除。
- `pct_chg > 8%`、`turnover_rate > 18%`、`volume_ratio > 4` 中命中两项及以上时直接剔除；只命中一项时降为风险候选，排序在所有普通候选之后。
- 风险候选仍必须通过绝对质量门槛；硬剔除标的不因候选不足递补。
- 阈值是待回测基线，不是已验证结论；README 已将其写入“设计中，尚未实现”的策略表。

### 用户范围提醒

- TradeEye 的其他工作流和底层能力同样可能需要优化，后续不能无限深入单个荐股阈值而忽略个股复盘、周报、新闻和工程可靠性。
- 荐股设计只再收敛“回踩结构破坏”和“三维评分骨架”两个必要问题；具体参数搜索留给历史回测，不在对接阶段继续手调。
- 随后切换到 `analysis` 的产品时点和算法，再完善周报指标；新闻、通知体验、数据链路、Actions、配置文档和测试作为独立优化轨道保留在 TODO。

### 全局优化轨道

1. **每日荐股**：完成最小交易闭环、质量与风险门、三维评分重构；不扩展为盘中系统。
2. **个股复盘/尾盘分析**：先解决当前 `15:30` 运行却可能读取上一交易日数据的时点错位，再决定它是盘后次日计划还是收盘前信号，并调整加减分与输出。
3. **周度评价**：落地信号层/组合层、交易状态、策略版本、净收益与风险指标，不只展示胜率。
4. **财经新闻**：后续检查来源健康、跨批次重复、相关性排序、空结果与失败状态，保持 RSS 批处理而非扩成资讯平台。
5. **数据与运行可靠性**：辅助数据降级、缓存、错误分类、退出码、信号原子写入和并发、配置校验、Actions 时点及依赖缓存。
6. **使用体验与文档**：统一 README、`.env.example`、Actions 变量、本地启动入口、通知内容和测试覆盖。

### 下一项设计分支

暂停继续细抠荐股阈值，转向 `analysis`：确认其第一版定位为“完整日线就绪后的盘后复盘与次日计划”，还是继续追求真正的收盘前尾盘信号。

### 当前边界

- 仍然只更新 README、`LOG.md` 与 `TODO.md`，未修改任何业务实现。

## 2026-08-23｜`analysis` 确认为盘后复盘与次日计划

### 已确认定位

- `analysis` 第一版改为读取完整日线后的盘后复盘与次日计划，不继续追求真正的收盘前尾盘信号。
- 该选择符合每天少量 GitHub Actions 批处理的项目约束，不需要引入准实时或分钟级行情。
- 实现时应安排到数据稳定时点之后运行；当前数据层以北京时间 `17:10` 作为快照就绪边界，因此约 `18:00` 是一个简单候选时点，精确 cron 尚未确认和修改。
- 当前状态名“尾盘隔夜”和行动文案“轻仓参与隔夜、次日高开兑现”等仍属于旧产品定义，实施时必须随算法一起调整，不能只挪运行时间。
- README 已新增“个股复盘改版（设计中，尚未实现）”，明确现有 `15:30` 调度尚未改变。

### 下一项职责边界提案

建议把 `analysis` 定义为 **自选股状态诊断**，而不是第二套自动荐股交易：

1. 输入仍是用户配置的 `MY_STOCKS`，其中可能同时包含持仓、观察标的和长期关注标的。
2. 输出聚焦行情日期、市场状态、个股趋势状态、主要正面因素、硬风险提醒和次日观察条件。
3. 在没有真实持仓数量、买入成本和用户风险承受信息时，不输出“加仓多少、止盈多少、必须卖出”等伪精确持仓指令。
4. LLM 只负责解释结构化规则结果和风险，不参与改变本地评分、风险门或状态结论。
5. `analysis` 不进入每日荐股的 5 槽位虚拟组合，也不混入其策略胜率；若未来需要评价诊断有效性，另设状态迁移或风险预警命中指标。

### 选择理由与取舍

- `recommend` 已负责候选筛选、条件入场和三日退出；若 `analysis` 也直接产生买入交易，两条工作流会出现信号冲突和重复统计。
- 诊断定位能保留自选股复盘价值，同时避免系统在不知道用户是否持仓、成本多少的情况下假装管理真实仓位。
- 代价是 `analysis` 不直接给出一笔可加入组合回测的交易；如果未来确实需要持仓管理，应单独设计带成本和仓位输入的 `portfolio` 模式，而不是暗中塞进复盘评分。

### 下一项设计分支

确认 `analysis` 是否只做自选股诊断与次日观察计划，不生成自动买入信号；确认后再收敛其状态分层和评分维度。

### 当前边界

- 仍未修改 Python、规则文件、Actions、信号格式或推送文案。

## 2026-08-23｜`analysis` 职责边界已确认，并启动评分系统重构讨论

### 已确认职责

- `analysis` 只做 `MY_STOCKS` 自选股的状态诊断、风险提醒和次日观察计划，不生成自动买入交易，也不占用每日荐股的 5 个组合槽位。
- 在不知道真实持仓、成本价和风险偏好的情况下，不输出伪精确的加仓、止盈和卖出指令。
- LLM 只解释本地结构化规则的结果，不能改变评分、风险等级或状态结论。
- README 与 TODO 已同步该职责；未来如需持仓管理，另行设计带持仓与成本输入的模式。

### 现行评分的结构问题

- 当前所有项目从 0 开始直接相加，默认正向规则理论上最多可累计约 156 分，最终再截断到 100；多种不同强度组合可能一起饱和到 100，`80/65/50` 分档缺少稳定含义。
- 收盘位置、涨跌幅、阳线实体和上影线共同描述当日价格行为；换手率、成交额/近 5 日、量比和换手排名共同描述活跃度；资金净流入、大单净额及两者横截面排名也高度相关，当前容易重复奖励同一现象。
- MA 排列、MA5 斜率和接近十日高点均反映趋势，独立累加会进一步放大相关特征。
- ST、新股和北交所等风险只是负分，可能被其他大量正分抵消；对诊断工具而言，“高结构分但高风险”应明确并列展示，而不是混成一个数字。
- 缺失数值目前常被转换为 0；例如换手率缺失会落入 `-8` 分支，`12%-18%` 的换手率也因现行分支结构落入同一 `-8`，数据缺失、普通区间和真实风险没有清楚区分。
- 市场环境直接与个股结构累加，难以看出个股本身强弱和大盘拖累分别贡献了什么。

### 建议的第一版评分架构

不继续在一个分数桶中追加规则，改成“数据质量门 + 五个有上限的维度 + 独立风险等级 + 状态矩阵”：

| 维度 | 分值上限 | 聚合内容 |
| --- | ---: | --- |
| 趋势结构 | 30 | MA 排列、短均线斜率、近十日位置/突破；相关特征在维度内合并，不重复跨维加分 |
| 收盘与价格行为 | 25 | 收盘位置、涨跌幅、阳线实体、上影线 |
| 量能与流动性 | 20 | 换手率、成交额相对近 5 日、量比；横截面排名只用于归一化或解释，不再额外叠加 |
| 资金确认 | 15 | 资金净流入与大单净额；绝对值和市场排名合成一个维度 |
| 市场环境 | 10 | 偏强、中性、偏弱背景，限制其对个股结构的影响幅度 |

- 总分仍为 `0-100`，但每个维度先在自己的预算内归一化，避免同一类指标无限堆分和最终大量截断。
- 关键行情或维度数据缺失时先标记数据质量，不把缺失机械当成 0 或负面；关键数据不足则不给综合状态。
- 风险从总分中拆出，形成低/中/高风险等级；最终状态由“结构总分 × 风险等级”共同决定，高风险不能靠其他维度高分完全抵消。
- 总分只表示当前结构健康度与关注价值，不等价于买入概率或预期收益。
- `80/65/50` 可暂时作为分档起点，但在历史样本中检查分布、区分度和后续状态后再调整，不预设旧阈值仍适用。

### 取舍与验证

- 五维方案保留现有绝大多数数据，Actions 次数和行情接口不增加；主要变化是去重、限额和解释方式。
- 权重 `30/25/20/15/10` 是可解释的第一版预算，不是历史数据证明的最优组合。
- 后续评价不以“高分是否一定上涨”作为唯一目标，应检查各分档样本量、分数饱和率、状态稳定性、次日/三日相对表现和风险预警命中率。
- 当前仅形成待确认架构，未修改代码、`rules.yaml`、测试、信号文件或 README 评分说明。

### 下一项设计分支

确认是否接受“趋势 30 + 价格行为 25 + 量能 20 + 资金 15 + 市场 10，风险等级独立”的评分骨架；确认后只需定义状态矩阵，不继续逐项手调全部阈值。

## 2026-08-23｜`analysis` 五维评分骨架已确认

### 已确认决策

- `analysis` 采用五个限额维度：趋势结构 30、收盘与价格行为 25、量能与流动性 20、资金确认 15、市场环境 10。
- 每个维度先在自身预算内归一化，再汇总到 `0-100`；横截面排名用于维度内归一化或解释，不再作为额外重复加分。
- 风险等级与结构总分分离，关键数据缺失先进入数据质量状态，不机械当成 0 或负分。
- 总分表示当前结构健康度与关注价值，不等价于买入概率、收益预测或自动交易信号。
- 权重只是待历史验证的第一版预算，不声称优于其他组合；README 已同步并继续标注尚未实现。

### 建议的最小状态矩阵

先按结构总分得到基础状态：

| 总分 | 基础状态 |
| ---: | --- |
| `80-100` | 结构强 |
| `65-79` | 结构偏强 |
| `50-64` | 结构中性 |
| `0-49` | 结构偏弱 |

再由独立风险等级约束最终状态：

| 结构分档 | 低风险 | 中风险 | 高风险 |
| --- | --- | --- | --- |
| 结构强 | 强势 | 观察 | 高风险 |
| 结构偏强 | 偏强 | 观察 | 高风险 |
| 结构中性 | 中性 | 谨慎 | 高风险 |
| 结构偏弱 | 偏弱 | 偏弱 | 高风险 |

- 低风险不改变基础状态；中风险把最终状态最高限制为“观察”；高风险直接显示“高风险”，但报告仍保留原始维度分，便于理解“结构强但风险高”。
- 关键数据不足时不进入矩阵，直接显示“数据不足”，避免伪精确分数。
- 风险等级第一版只需遵循简单聚合：无主要风险为低风险；一项主要风险或多项一般警告为中风险；硬风险或两项以上主要风险为高风险。具体阈值沿用可配置规则并交给后续回测，不在对接阶段逐项手调。
- 输出同时展示总分、五维分、风险等级、最终状态、主要依据和次日观察条件；状态名称不包含买入、加仓或卖出指令。

### 选择理由与限制

- 状态矩阵防止高结构分掩盖高风险，同时没有丢掉维度信息，适合诊断工具而非交易信号器。
- 暂沿用 `80/65/50` 作为研究起点只是为了减少第一版变量；正式实现后应检查分数分布是否过度集中，再调整分界。
- 当前是待确认设计，没有修改 Python、规则配置、测试、信号格式或通知输出。

### 下一项设计分支

确认是否采用上述状态矩阵；确认后 `analysis` 算法设计先封口，后续再单独处理 LLM 调用条件和报告版式，避免继续深挖分数细节。

## 2026-08-23｜建议改版后的 `analysis` 完全移除 LLM

### 用户提出的问题

- 需要考虑以分数门槛减少 `analysis` 的 LLM 调用成本，或者判断该流程是否已经完全不需要 LLM。
- 用户倾向于认为此处 LLM 没有实际必要。

### 两种方案比较

1. **保留分数门槛**：延续当前“高分调用 LLM、低分使用本地模板”。优点是高分报告仍有自然语言扩写；缺点是继续保留两套报告路径、API 成本、网络失败和超时、门槛配置及输出不一致。
2. **`analysis` 完全本地化**：所有股票统一由确定性模板输出五维分、风险等级、状态、主要依据和次日观察条件。优点是零 LLM 成本、结果可复现、无外部模型失败、所有分档格式一致；缺点是语言表达不如自由生成灵活。

### 推荐决策

- 建议 `analysis` 第一版完全不调用 LLM，而不是再设计一个新分数门槛。
- 五维结构、风险等级和状态矩阵已经足以生成有用报告；LLM 不能增加新的行情事实，也不应改变本地诊断结论。
- 使用结构化本地模板列出维度得分、主要正面因素、主要风险和次日观察条件，能够覆盖该工作流的实际用途。
- 该决定只针对 `analysis`；`recommend` 当前的 LLM 点评暂不联动删除，后续单独评估其信息增量与成本。

### 后续实现影响（尚未执行）

- `tradeeye.app` 不再接收或调用 `analyzer`，所有分数统一走本地报告生成器。
- `analysis.llm_score_threshold` 失去用途，应在迁移时移除或弃用；信号字段 `called_llm` 可先恒为 `false` 兼容旧 CSV，再评估下一个格式版本移除。
- `LLM_API_KEY`、模型地址和超时仍可供 `recommend` 使用，不能因 `analysis` 本地化而从全项目配置中直接删除。
- 测试应从“高低分是否走不同 LLM 分支”改为验证五维报告、风险状态、缺失数据和确定性输出。
- 当前只是待确认设计，没有修改代码、配置、信号格式、测试或 README 的现有功能说明。

### 下一项设计分支

确认改版后的 `analysis` 是否完全不调用 LLM；状态矩阵的确认随后恢复，不再增加新的评分阈值讨论。

## 2026-08-23｜`analysis` 去 LLM 已确认，并建议 TradeEye 全项目去 LLM

### 已确认决策

- 改版后的 `analysis` 完全不调用 LLM，不再保留基于分数的调用门槛。
- 所有自选股统一由确定性本地模板输出五维得分、风险等级、最终状态、主要依据和次日观察条件。
- 现有代码仍未修改；README 设计区已将该项从“评估中”更新为“已确认”，同时保留对当前 `score >= 70` 行为的事实说明。

### 四条工作流实际依赖核查

| 工作流 | 是否调用 LLM | 对业务结果的作用 |
| --- | --- | --- |
| `analysis` | 是 | 当前仅对达到门槛的本地评分结果做语言扩写；不产生行情、特征或分数 |
| `recommend` | 是 | 筛选、三维评分、分组、信号记录全部完成后才调用，只生成点评文本 |
| `news` | 否 | RSS/Atom 抓取、过滤、去重、排序和模板推送均为本地确定性逻辑 |
| `backtest` | 否 | 信号加载、行情查询、收益和胜率汇总均为本地确定性逻辑 |

### 推荐的全项目决策

- 建议 TradeEye 完全去 LLM：`analysis` 按已确认方案本地化，`recommend` 也删除 LLM 点评，`news` 与 `backtest` 保持现状。
- `recommend` 当前已有 `build_recommendation_brief()` 本地摘要；后续只需扩充为候选分组、三维分、风险标签、计划入场价、有效期和策略说明，无需模型重新描述同一批字段。
- 去掉荐股 LLM 不改变候选筛选、排名、Top5 上限或信号 CSV，因为这些步骤本来就在 LLM 调用前完成。
- 项目会获得零 LLM 调用成本、稳定输出、无需模型密钥、减少超时和外部故障、报告可测试可回放等收益。
- 代价是少了自由生成的市场点评，但现有提示词本身要求模型根据固定字段作确定性判断，信息增量有限；对自动化研究项目而言不值得维持整条模型链路。

### 后续实现影响（尚未执行）

- 移除 `tradeeye.services.analysis` 中两套 LLM 提示词、请求与解析逻辑，并移除两个 app 的 analyzer 注入分支。
- 从运行配置、`.env.example`、Actions 和 README 部署说明中移除 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`、`LLM_TIMEOUT_SEC`；历史设计文档是否保留旧事实应单独处理，不直接抹除历史。
- `analysis.llm_score_threshold` 和 `called_llm` 进入兼容迁移；旧 CSV 可读取，新格式不再生成无意义字段。
- 删除 LLM 专属测试，增加本地分析报告与荐股报告的内容、空结果、风险标签和确定性测试。
- `requests` 仍被飞书通知和 RSS 请求使用，不能仅因去 LLM 而从依赖中删除。
- Tushare 和飞书仍是外部服务；“全项目去 LLM”只代表模型调用成本归零，不代表所有运行成本和外部依赖归零。

### 下一项设计分支

确认 TradeEye 是否全项目去 LLM，包括删除 `recommend` 的 LLM 点评；确认后恢复状态矩阵确认，并把去 LLM 工作纳入最终实施计划。

## 2026-08-23｜TradeEye 全项目去 LLM 已确认

### 已确认范围

- `analysis` 删除高分 LLM 解读，全部使用确定性本地诊断报告。
- `recommend` 删除选股后的 LLM 点评，使用本地模板展示分组、三维分、风险标签、计划入场价、有效期和策略说明。
- `news` 与 `backtest` 本来不调用 LLM，保持确定性本地逻辑。
- 最终目标是 TradeEye 业务路径中不再发起任何模型请求，也不再要求模型 API 密钥。

### 实施边界

- 当前仍是设计确认，没有立即修改业务代码和配置；README 对“现有功能”的描述继续保留当前事实，去 LLM 只在“设计中”部分标为已确认。
- 后续实施计划必须同步处理 app 调用链、LLM 服务代码、配置与 Actions 变量、环境变量样例、信号兼容、README 和测试，避免只删除请求函数留下死配置。
- Tushare 行情与飞书通知继续保留，`requests` 仍有其他用途。

### 下一项设计分支

恢复确认 `analysis` 的最小状态矩阵；完成后封口其评分结构并切换到周报、新闻或工程可靠性轨道。

## 2026-08-23｜`analysis` 状态矩阵已确认，评分结构封口

### 已确认决策

- 结构总分分为 `80-100` 结构强、`65-79` 结构偏强、`50-64` 结构中性、`0-49` 结构偏弱。
- 低风险保留基础结构状态；中风险将最终状态最高限制为“观察”；高风险直接显示“高风险”。
- 风险限制最终状态时仍保留总分和五维分，允许读者理解“结构较强但风险较高”。
- 关键数据不足时显示“数据不足”且不评分，不生成伪精确结果。
- README 已同步矩阵；`analysis` 的产品定位、职责、五维评分、风险隔离、状态矩阵和去 LLM 设计至此封口。
- 具体指标阈值、风险映射和报告排版进入后续实现与历史验证，不继续在调研对接阶段逐项手调。

## 2026-08-23｜周度评价第一版最小报告提案

### 范围纠正

- 新交易周报只评价 `recommend` 产生并按统一策略触发的交易，不再把 `analysis` 自选股诊断分档假设成买入信号后混入胜率。
- `analysis` 若未来需要验证，应另看分档样本分布、状态区分度和风险预警命中率，不进入 5 槽位组合收益。
- 周报继续使用每周一次的 `backtest` Action，从已持久化状态和日线行情汇总，不增加每日运行次数。

### 建议的报告结构

1. **本周活动概览**
   - 发布推荐数、`D+1` 入场触发数、触发率、已退出数、未结算数、数据缺失数。
   - 明确策略版本、报告区间、统一往返成本 `0.15%` 和每组 Top5 只是上限。
2. **信号层结果**
   - 已结算笔数、盈利笔数、扣费后胜率、平均净收益、中位数净收益。
   - 平均盈利、平均亏损、盈亏比、最大单笔亏损。
   - 止盈、止损、`D+3` 超时和跳空退出的数量分布。
3. **组合层结果**
   - 实际新开仓与已退出笔数、本周已实现组合贡献，即每笔净收益按单槽 `1/5` 权重计入。
   - 平均槽位使用率、周末未结算持仓数，以及因满仓或每日最多新开 2 只而跳过的信号数。
4. **滚动窗口对照**
   - 除本周外，保留当前 `BACKTEST_LOOKBACK_DAYS`（默认 45 天）的同口径滚动结果，避免一周样本过少导致胜率剧烈摆动。

### 明确暂不进入第一版

- 不加入年化收益、夏普比率、复杂基准组合、蒙特卡洛或真实资金/整手撮合。
- 不把尚未退出的持仓提前判输赢；组合层的“已实现贡献”不能冒充包含未实现盈亏的完整净值收益。
- 完整日度净值曲线与最大回撤等到交易账本稳定后再评估，避免第一版周报反过来逼出复杂账户引擎。

### 选择理由与限制

- 这组指标能同时回答“有多少推荐真正触发”“交易规则是否有正期望”“5 槽位约束后的实际利用率如何”，比单独胜率更难被样本缩减误导。
- 本周与滚动 45 天并列能兼顾近期变化和样本稳定性，但仍属于研究统计，不代表未来收益。
- 当前只是待确认提案，没有修改 `backtest`、信号文件、交易状态或 Actions。

### 下一项设计分支

确认是否采用上述最小周报；确认后周报指标封口，不继续加入高级量化指标，转向新闻或工程可靠性调研。

## 2026-08-23｜第一版不展示年化，但必须预留连续绩效数据

### 用户提出的问题

- 第一版若暂不展示年化收益，后续需要年化时是否仍能计算，以及现在是否必须提前保留数据。

### 结论

- 必须从第一版开始保留连续日度组合净值。只有已平仓交易记录无法正确反映空仓、重叠持仓、跨周未结算仓位和资金利用率，不能据此生成可信的组合年化与最大回撤。
- “暂不在周报展示高级指标”与“保留未来可计算的数据”不冲突；前者控制产品复杂度，后者避免未来因历史缺口无法补算。

### 建议的最小持久化集合

1. **信号表**
   - 稳定 `signal_id`、策略版本、信号日期、代码、价格组、维度分、风险标签、信号日收盘、计划入场价和到期日。
2. **交易账本**
   - 稳定 `trade_id` 与关联 `signal_id`、组合槽位、触发/入场日期与价格、退出日期与价格、退出原因、毛收益、`0.15%` 成本、净收益、当前状态和未纳入组合的跳过原因。
3. **日度组合净值**
   - 组合序列 ID、策略版本、交易日期、归一化净值、日收益、已实现贡献、未实现贡献、占用槽位、现金槽位、开放交易 ID 和数据质量状态。

### 简化实现原则

- 不需要引入真实初始资金：组合从净值 `1.0` 开始，每个槽位权重固定为 `1/5`，空槽收益为 0。
- 每个交易日完整日线就绪后只追加一条组合净值；最多 5 个持仓、最长 3 天，状态量很小，不增加 GitHub Actions 运行频率。
- 信号与交易数据采用追加式、可追溯记录；状态更新需要原子写入和并发保护，不能继续依赖无锁整表覆盖。
- 规则发生变化时生成新的策略版本/组合序列，报告不得把不同版本无标记拼接后计算年化。
- 当前主工作流只提交 `data/signals/`；未来实施时必须扩展持久化范围并处理 Actions 并发，当前尚未修改。

### 未来指标能力

- 有连续日度净值后，未来可以按实际观察交易日数计算累计收益，并在样本足够时计算年化收益、年化波动率、最大回撤和净值曲线。
- 年化收益可基于 `期末净值 / 期初净值` 与实际交易日数换算；第一版不急于展示，且任何年化数字必须同时注明样本起止日期、交易日数和策略版本。
- 样本很短时不展示或醒目标注年化，避免把数周结果外推成稳定年度能力。

### 当前状态

- README 与 TODO 已加入该数据保留原则；目前只是设计，没有创建新的数据文件或修改工作流。

### 下一项设计分支

确认“信号表 + 交易账本 + 每日一条组合净值”是否作为第一版必须持久化的数据；确认后再一并确认最小周报展示指标。

## 2026-08-23｜连续绩效数据底座已确认为第一版必需

### 已确认决策

- 第一版必须持久化追加式信号表、交易账本和连续日度组合净值，不能只保存已平仓结果。
- 组合以归一化净值 `1.0` 和 5 个等权槽位运行，每个完整交易日只记录一条组合净值，不引入真实初始资金。
- 所有信号、交易和净值均保存策略版本；策略规则变化时开启新的组合序列，禁止无标记拼接不同版本计算长期表现。
- 第一版可以不展示年化、波动率和最大回撤，但必须保证未来能够基于完整历史补算，并同时报告实际观察区间和交易日数。
- README 已把该项从“设计中”更新为“已确认，尚未实现”。

### 当前边界

- 尚未创建数据文件、迁移现有 CSV 或修改 Actions 持久化范围；具体表结构、兼容迁移、原子写入和并发处理进入实施计划。

### 下一项设计分支

确认第一版周报实际展示的最小指标集；保留数据的范围可以大于当前展示范围。

## 2026-08-23｜第一版周报展示指标已确认

### 已确认决策

- 交易周报只评价 `recommend`，`analysis` 自选股诊断不混入交易胜率。
- 并列展示本周和 `BACKTEST_LOOKBACK_DAYS`（默认 45 天）滚动结果，未退出交易保持未结算。
- 信号层展示推荐数、触发率、已结算数、扣费后胜率、平均/中位净收益、平均盈利、平均亏损、盈亏比、最大单笔亏损和退出原因。
- 组合层展示已实现组合贡献、槽位使用率、周末未结算持仓和因满仓/每日开仓上限跳过的数量。
- 年化、夏普和完整净值回撤不进入第一版展示，但信号表、交易账本和日度净值已经确认为必留数据。
- README 已同步；周报产品指标至此封口，具体字段和计算测试进入实施计划。

## 2026-08-23｜`news` 现状优化审视与定位提案

### 已核实现状

- 仓库内置 7 个 feed，其中 6 个来自国际能源网的电力/能源栏目，另 1 个为中国新闻网财经 RSS。
- Actions 默认包含关键词集中在电力、电网、储能、火电、水电、核电、风电、光伏和新能源，生产调度实质上是电力能源专题简报。
- README、模板标题和代码默认标题使用“财经新闻简报”，容易让使用者误以为覆盖全面泛财经。
- `.env.example` 的包含关键词默认为空，本地运行会接收内置 feed 中所有内容；本地与 Actions 默认口径不一致。
- 当前只在同一批次内按链接或标题时间去重，没有持久化已发送指纹，跨日重复新闻可能再次推送。
- 多个源中单个失败会记录异常后继续，但收集结果没有保留成功/失败源统计；所有源失败时最终可能与“成功抓取但没有匹配新闻”表现相同。
- 排序只按发布时间倒序，不衡量关键词命中强度、来源优先级或标题相关性。
- 无法解析发布时间时当前回退为“现在”，可能把时间异常的旧文章当成最新内容。

### 建议的产品定位

- 建议将 `news` 正式定义为 **可配置主题简报**，默认主题为“电力能源”，而不是承诺覆盖全面泛财经。
- 默认 feed、默认关键词、README、模板标题和 Actions 使用同一电力能源口径；用户仍可通过 `NEWS_RSS_FEEDS`、feed 文件和包含/排除关键词切换成其他主题。
- 不引入多套复杂 profile 系统，也不使用 LLM 摘要；第一版只把现有配置能力的产品含义说清楚。

### 后续最小优化方向

1. 持久化最近已发送新闻指纹，做跨批次去重。
2. 返回每个来源的成功、失败、抓取条数和匹配条数；所有源失败应作为故障，成功但无匹配才是业务无结果。
3. 使用确定性关键词命中数与发布时间做轻量相关性排序，不只按时间排列。
4. 无法解析发布时间时标记时间未知或过滤，不再默认伪装成最新文章。
5. 补齐跨批次去重、全源失败、部分降级和时间异常测试。

### 当前边界

- 目前只是调研与定位提案，没有修改 feed、关键词、模板、代码、Actions 或测试。

### 下一项设计分支

确认 `news` 是否定位为“可配置主题简报，默认电力能源”，而不是泛财经聚合器。

## 2026-08-23｜`news` 定位已确认并降为独立可选接口

### 已确认决策

- `news` 独立成栏，不进入荐股、复盘、交易账本或周报主链，最多作为一个可选资讯接口。
- 产品口径采用“可配置主题简报，默认电力能源”，不宣称覆盖全面泛财经。
- 当前飞书 webhook 只能单向推送，不能查询、追问或动态调整订阅；在具备交互式飞书机器人前，该模块业务价值有限，维持低优先级。
- 暂不投入复杂摘要、主题系统或资讯排序产品；跨批次去重、来源健康、全源失败分类和异常时间处理作为接口可靠性待办保留。
- README 已把功能名改为“主题简报接口（可选）”并独立说明边界，当前代码和默认配置尚未修改。

## 2026-08-23｜每日荐股三维评分重构提案

### 现行结构错位

- `long_value` 以行业内低 PE 前 40% 且高市值前 20% 为命中条件，与已确认的 3 日短线回撤策略没有直接时间尺度对应；它可能描述公司估值或规模，但不能自然解释次日回踩后的短线机会。
- `short_burst` 同时奖励涨幅、量比和换手，`t_active` 又奖励振幅和成交额，两者大量描述同一天的量价活跃，存在相关特征重复加分。
- 当前任一维度命中即可入池，随后按 `0.4/0.3/0.3` 加权；单独命中 `long_value` 的标的也可能因组内缺少候选进入 Top5，与“绝对质量门槛、不凑数”决策冲突。

### 建议的三维骨架

仅使用当前全市场快照已有字段，不增加行情接口或 Actions 次数：

| 新维度 | 分值上限 | 主要信息 |
| --- | ---: | --- |
| 短线动能 | 40 | 当日涨幅的合理区间、相对全市场强度、短线爆发程度；过热部分交给已确认风险门 |
| 收盘质量 | 35 | 收盘位置、阳线实体、上影线和振幅结构，区分强势收盘与冲高回落 |
| 量能与资金确认 | 25 | 换手率、量比、成交额、资金净流入与大单承接；绝对值与横截面排名在维度内合成，不重复叠加 |

### 对旧维度的处理

- 删除 `long_value` 正向评分，不再让 PE 和行业市值排名决定 3 日短线推荐；行业、PE 和市值可保留为报告元数据或后续风险/流动性研究字段。
- `short_burst` 的有效信号并入“短线动能”，“t_active”拆入“收盘质量”和“量能与资金确认”，不再以命中标签额外奖励同一现象。
- 总分为三个限额维度之和 `0-100`，风险门独立先行；Top5 继续只是通过绝对质量门槛后的数量上限。
- `RECOMMENDER_INDUSTRIES` 若不再参与评分，实施时应弃用或重新定义，不能留下看似生效但实际无效的配置。

### 选择理由与限制

- 新骨架直接服务“昨日强势候选、次日固定回撤、最多持有 3 日”的时间尺度，并复用现有快照字段，保持批处理简单。
- 权重 `40/35/25` 是可解释的第一版预算，不是已验证最优值；绝对质量门槛和维度内部阈值应通过历史样本分布与双层结果确定，不继续凭感觉反复手调。
- 估值信息被移出短线正向分不等于其永久无价值；若未来增加中长期观察池，应另设不同目标和评价窗口，不能混入当前每日荐股。
- 当前只是待确认设计，没有修改 Python、`rules.yaml`、信号格式、README 策略表或测试。

### 下一项设计分支

确认是否用“短线动能 40 + 收盘质量 35 + 量能资金 25”替换现行 `short_burst / t_active / long_value`，并从 3 日短线正向评分中移除 `long_value`。

## 2026-08-23｜每日荐股三维评分骨架已确认

### 已确认决策

- 每日荐股改为短线动能 40、收盘质量 35、量能与资金确认 25，三个维度合计 `0-100`。
- `long_value` 不再参与三日短线正向评分；行业、PE 与市值只保留为报告元数据或未来中长期策略研究字段。
- `short_burst` 的有效信息并入短线动能，`t_active` 拆入收盘质量和量能资金，取消旧多维命中奖励，避免同一日活跃特征重复加分。
- 已确认的一日游风险门继续独立于总分；Top5 仍只是通过绝对质量门槛后的数量上限。
- 新骨架只复用现有全市场快照字段，不增加行情接口、分钟数据或 Actions 运行次数。

### 回踩结构问题同步封口

- 先前已经确认第一版不设置低开取消阈值，也不要求盘中企稳确认：`D+1` 最低价触及 `D` 收盘价的 `98%` 即按既定日线成交假设入场，未触及则当日过期。
- 因此第一版不再另设“回踩结构破坏”分支。信号日的一日游风险门负责事前过滤，入场后的固定止损、固定止盈与 `D+3` 超时规则负责结果管理。
- 这是为了保持每天 2-3 次批处理的确定性基线，不代表低开取消无研究价值；待积累历史结果后，再比较增加该规则是否改善净收益与最大不利波动。

### 下一项设计分支

确认计分形式是否采用“维度内只做封顶加分、风险门在总分外单独否决或降级”。该形式避免同一风险既扣总分又触发否决；具体阈值和最低入选分仍交给历史回测，不在对接阶段手调。

## 2026-08-23｜荐股计分形式确认，调研对接封口

### 已确认决策

- 荐股三个维度只在各自 `0-上限` 内做正向加分，弱特征记为不加分，不再在总分中混入负分。
- 明显冲高回落与过热风险继续由独立风险门负责硬剔除或风险降级；同一形态不能既扣总分又触发否决。
- 总分只表达候选的正向结构质量，风险层表达能否进入排序以及排序优先级，两者在报告和持久化数据中分别展示。
- 具体内部阈值、最低入选分及其优劣仍属于待历史验证参数，不在本轮对接中凭感觉确定最优值。

### 实施交接

- 四条功能线、三日荐股交易契约、组合约束、周报统计、连续数据保留、全项目去 LLM 和 `news` 边界已经形成足够明确的第一版设计。
- `TODO.md` 已按依赖顺序补充分阶段实施计划：先锁定版本化数据契约与兼容测试，再依次改荐股、交易状态、复盘、周报、Actions 与文档，最后处理低优先级 `news` 可靠性。
- 回归清单覆盖评分与风险、入场退出、5 槽位组合、幂等持久化、双层周报、五维复盘、故障降级、Actions 时点和全项目去 LLM。
- 当前仍只修改 README、`LOG.md` 与 `TODO.md`，没有修改 Python、规则、配置、Actions、测试或生产数据；实施必须等待用户后续明确授权。

## 2026-08-23｜统一股票池、低价软偏好与 ETF 扩展边界已确认

### 用户确认的目标

- 取消 `0-10` 元、`10-20` 元两个硬价格组，不再把 20 元以上股票自动排除在新策略之外。
- 股票改为统一候选池，风险门和绝对质量门先行，每日最多发布 5 条信号；数量上限不是配额，允许少推或不推。
- 保留“最好偏向低价股”的产品偏好：默认以 20 元及以下为偏好区间，通过独立的小幅排序加成体现，不改变短线动能、收盘质量和量能资金构成的原始 `quality_score`。
- 硬最低价、硬最高价、软偏好边界、软偏好加成和每日发布上限全部进入版本化策略配置；改变这些值必须留下策略版本，不能依靠无版本记录的环境变量静默改变历史口径。
- ETF 作为可选扩展保留，默认关闭并采用白名单；启用后使用独立评分、策略版本、报告和验证样本，第一版不与股票统一混排或拼接净值。

### ETF 数据核查

- Tushare 提供独立的 [`etf_basic`](https://tushare.pro/document/2?doc_id=385) 与 [`fund_daily`](https://tushare.pro/document/2?doc_id=127) 接口；ETF 日线可提供开高低收、涨跌幅、成交量和成交额。
- 股票当前使用的 `daily_basic` 与个股 `moneyflow` 字段不能假设在 ETF 日线上同样存在，因此 ETF 第一版改用动能 45、收盘质量 35、流动性 20，不能直接套股票 `40/35/25`。
- 官方当前文档显示 ETF 接口存在独立积分/权限要求；实现必须在无权限或 ETF 数据失败时只关闭 ETF 分支，不能拖累股票荐股。

### 配置与实现边界

- 目标配置放在 `rules.yaml` 并随策略版本进入信号记录；`.env` 继续只承载凭据、通知和部署输入。
- 当前 `rules.py` 合并器只覆盖数字与固定长度数值元组，实施时必须补齐字符串、布尔、列表、枚举和空值的严格解析与校验。
- ETF 复用早晨批次，不增加 Actions 次数；默认关闭意味着股票第一版可以独立完成并验证，再启用 ETF 白名单。
- README 已同时写明“当前仍是两组 20 元硬上限”和“已确认但尚未实现的目标”，避免把配置草案误当成已经生效。

### 缺口审计新增事项

- `.gitignore` 当前只放行 `data/signals/*.csv`，未来交易账本与净值文件需要显式放行。
- 当前信号写入返回值未被 app 检查，且飞书失败会阻止工作流后续数据提交；实施时必须解耦数据持久化与通知结果。
- 下一项只需确认停牌、个股缺行情、到期日无报价和无法成交时的最小状态推进规则。

### 当前边界

- 本轮仍只更新 README、`LOG.md` 与 `TODO.md`，未修改 Python、`rules.yaml`、Actions、`.env.example`、测试或生产数据。

## 2026-08-23｜停牌、缺行情与无法成交状态规则已确认

### 已确认规则

- 市场交易日历负责确定 `D+1 / D+2 / D+3`；市场休市只顺延交易日，不记作个股无行情。
- 主行情接口整体失败或无法确认完整性时，本批次失败并等待重试，不推进信号、交易、槽位或净值状态。
- 市场行情正常但 `D+1` 个股没有有效 OHLC 时，该信号终止为 `entry_unavailable`，不延期追入，也不计入触发率、胜率或收益分母；周报单列。
- 已持仓标的到计划退出日仍无有效报价时写入 `exit_deferred`，继续占用槽位，实际持有期允许超过计划 3 日。
- 延期期间用最后一个有效收盘价进行净值估值并标记 `stale_price`；该估值不是成交价，不能制造已实现收益。
- 到期后首个存在有效行情的交易日按开盘价退出；若开高低收全部等于跌停价，按一字跌停无法卖出处理并继续顺延。

### 数据与报告影响

- 信号状态必须区分 `entry_unavailable`、`not_triggered`、`provider_error` 和市场休市，不能都归为“缺失行情”。
- 交易账本保存计划到期日、实际退出日、延期交易日数、延期原因和退出价格来源。
- `entry_unavailable` 与 `exit_deferred` 均单列计数；延期未退出交易不进入已结算胜率分母。
- 组合层在延期期间保留槽位占用，不能假设 `D+3` 自动释放后把同一资金分给新信号。

### 选择理由与限制

- 该口径不把 Tushare 故障误判成停牌，也不为了保持“三日持有”外观而虚构无法完成的卖出。
- 日线只能识别明显的一字跌停，不能证明普通跌停日某一卖单必然成交；第一版采用上述可复现保守假设，并在报告中披露模型限制。
- 当前仍是设计确认，没有修改行情调用、交易状态、账本、周报、测试或 Actions。

### 下一项设计分支

确认组合是否需要同一行业持仓硬上限。建议第一版只记录行业分布与最大行业占比，不设硬上限；待样本证明集中度确实恶化组合结果后再加入限制。

## 2026-08-23｜第一版不设行业持仓硬上限

### 已确认决策

- 第一版不限制同一行业最多占用多少个槽位；合格候选仍按风险层、质量门、价格偏好和总分竞争空槽。
- 信号与交易必须保留行业标签，组合周报展示各行业持仓数量、最大行业占比和行业未知数量。
- 行业字段缺失统一标为“未知”，不能把每个缺失值伪装成不同类别，从而低估集中度。
- 行业集中度第一版仅用于观察和评价，不反向修改交易结果或净值；样本积累后再判断是否增加例如同业 2 只上限。

### 选择理由与限制

- 5 个槽位和每天最多新开 2 只已经限制了第一版组合规模；在没有样本时再叠加行业硬上限，可能把优质的主题性行情直接过滤掉。
- 不设硬上限不代表忽略集中风险，因此集中度字段和周报必须从第一版保留，不能等未来才发现历史没有行业数据。
- 当前仍只更新 README、`LOG.md` 与 `TODO.md`，没有修改业务代码、配置、Actions、测试或生产数据。

### 当前状态

- 37 项待对接产品与策略决策均已确认；后续剩余工作全部属于尚未获准执行的实现、迁移、验证和低优先级优化。

## 2026-08-24｜`recommend_v2` 与确定性研究主链实现完成

### 实现结果

- 每日荐股已切换为统一股票池和 `40/35/25` 三维封顶加分，风险门独立执行；每日最多 5 条但允许少推或不推，20 元及以下只获得可配置排序偏好。
- ETF 已作为默认关闭的白名单接口接入同一早晨批次，采用独立 `45/35/20` 评分、策略版本、报告和信号文件；关闭时不发起 ETF 数据请求。
- 新增荐股三日状态机与虚拟组合：D+1 固定 2% 回踩、T+1、D+2/D+3 止盈止损、D+3 超时、0.15 个百分点统一成本、5 个槽位和每日最多 2 笔新仓。
- 组合容量按开盘前状态决定，当日退出不追溯释放给当日新仓；不强制满仓，同股不重复，行业只记录集中度。
- `entry_unavailable`、`exit_deferred`、停牌/缺行情、一字跌停延期和陈旧估值均有独立状态；主行情不完整时整批失败，不错误推进交易和净值。
- `data/signals/recommend.csv`、`data/trades/recommend_trades.csv` 与 `data/portfolio/recommend_nav.csv` 使用稳定 ID、策略版本和幂等增量写入；NAV 保留 `settled_through` 水位，为非交易日精确重跑和未来年化/回撤保留输入。
- 周报只评价 `recommend`，按策略版本同时展示信号层和组合层的本周与滚动窗口；跨周退出按实际退出日归属，开放交易不进入胜率。
- `analysis` 已改为 `30/25/20/15/10` 五维限额评分和独立风险矩阵；关键数据不足时不评分，只生成盘后诊断与次日观察点。
- 全项目运行链路已移除 LLM 调用和配置；荐股、盘后诊断、周报与新闻均保持确定性本地处理。

### 运行与文档同步

- 核心 Actions 调整为北京时间工作日 06:00 荐股、18:00 先结算再诊断；周五 19:30 周报会先验证当天晚间批次成功，新闻继续保持独立低优先级批次。
- 晨报日期显式使用 Asia/Shanghai，不依赖 UTC runner；飞书 HTTP 200 仍会校验业务成功码，RSS 全源失败返回非零状态。
- 新增独立 push/PR CI；业务 Actions 只安装运行依赖，并用统一并发组串行化写数据任务。
- README、`.env.example`、`rules.yaml`、Windows BAT、本地入口、依赖分层和数据文件说明均已同步；过时的 `file_list.txt` 已删除。
- `TODO.md` 已从“等待实施”改为上线观察、样本校准、未来绩效指标和低优先级新闻可靠性事项。

### 验证证据

- `python -m pytest`：148 项全部通过。
- `python -m compileall -q tradeeye main.py recommend_main.py news_main.py backtest_main.py portfolio_main.py config.py`：通过。
- `git diff --check`：通过。
- 验证使用固定假数据，没有连接真实券商、执行下单或发送生产飞书消息。

### 保留边界

- 当前最低质量分、风险阈值和 2% 回踩属于可解释的第一版基线，不宣称经过历史参数寻优。
- 年化、夏普、完整回撤、指数基准、MFE/MAE 和 ETF 独立绩效序列继续留待样本积累后实现。
- GitHub 仓库中旧 LLM Secrets/Variables 需要由仓库管理员在设置页手动删除；本地代码不读取这些配置。

## 2026-08-24｜GitHub Actions 结算与 CI 故障修复

### 故障证据

- `TradeEye Core Automation #226` 失败在 `Settle recommendation trades and NAV`，尚未执行盘后诊断和飞书推送；真实异常为 `920176.BJ` 在 `20260727` 缺少有效 `down_limit`，不是飞书发送失败。
- `TradeEye CI #1` 失败在测试步骤：clean checkout 不存在被忽略的 `.tmp/`，而 `--basetemp=.tmp/pytest` 不会代建父目录，造成 43 个 `tmp_path` 用例报 `FileNotFoundError`。

### 修复结果

- Tushare 日线或涨跌停接口整批不可用、以及整日没有任何可安全结算报价时仍保持失败，不推进账本。
- 只有个别代码缺少有效跌停价时，记录告警并保守跳过该股报价；其他代码继续结算。缺失代码不会被误判为可卖出，D+1 按无报价终止入场，已持仓按现有延期规则处理。
- pytest 临时目录改为仓库根目录下可直接创建的 `.pytest-tmp`，并加入忽略规则，不再依赖 checkout 中不存在的父目录。

### 验证证据

- 新增部分跌停价缺失降级、整批仍须失败和 clean checkout 临时目录回归用例。
- Windows/Python 3.13：`150 passed`。
- 无 `.tmp/` 的 Linux/Python 3.13 clean checkout 模拟：`150 passed`，随后 `compileall` 通过。
- 推送后的真实 `TradeEye CI` 已通过；手动 `settle` 生产任务也已成功结算到 `20260824`，写入 210 条交易状态与 22 条 NAV。日志中的 9 个北交所跌停价缺失均按单股告警降级，没有再阻断整批。
- 本次用 `settle` 模式验证原故障路径，未主动补发盘后飞书消息；飞书步骤留给下一次正常 `evening` 批次验证，避免产生重复通知。
